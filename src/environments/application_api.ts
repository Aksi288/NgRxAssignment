let users: string = `usersData/`;
let auth : string = `auth/`;

export default {

    users: {
      list: `${users}`,
      details: `${users}#/`,
    },
    auth:{
      login :`${auth}login/`
    },
   authUer:{
      me :`me/`
    }

}